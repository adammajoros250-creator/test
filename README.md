## Installation

1. Clone the repository
2. Install the dependencies on the project folder: `npm install:all`

## Usage

1. Run the development server: `npm start`
2. Access Mangaverse in your web browser: `http://localhost:5173`